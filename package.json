{
  "name": "@sleepymug/ts-executor",
  "version": "0.4.0",
  "description": "Execute checked TypeScript functions in fresh Node subprocesses, with local and host-backed modules.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "docs",
    "examples"
  ],
  "engines": {
    "node": ">=18.19.0"
  },
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist', { recursive: true, force: true })\"",
    "build": "pnpm run clean && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.js",
    "check": "pnpm run typecheck && pnpm test",
    "examples": "pnpm run build && node examples/run-all.mjs",
    "prepack": "pnpm run build"
  },
  "keywords": [
    "typescript",
    "executor",
    "subprocess",
    "json"
  ],
  "license": "MIT",
  "dependencies": {
    "@types/node": "^18.19.0",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "esbuild"
    ]
  }
}
